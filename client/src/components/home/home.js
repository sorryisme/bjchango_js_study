import React from 'react';
import { useApolloClient } from '@apollo/react-hooks';
import { List, ListItem, Text, Icon, Left, Right, Body, Button } from 'native-base'; 

const HomeScreen =  ({ navigation }) => {


    
    return (
        <List>
            <ListItem>
                <Text>test</Text>
            </ListItem>
        </List>
    );
}

export default HomeScreen;